import { EFeatureRequirements } from "../types/FeatureRequirements";
import { Route } from "../types/Route";

export const checkFeatureRequirements = (requirements: EFeatureRequirements[], route: Route): boolean => {

  return false;
}