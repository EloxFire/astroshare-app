import { globalStyles } from "../../helpers/globalStyles";
import { app_colors } from "../../helpers/variables";

export const exploreScreenStyles = {
  screen: globalStyles.screen,
  header: {
    height: 20,
    backgroundColor: app_colors.primary.main,
  },
};
